apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: digital-library-ingress
spec:
  rules:
  - host: library.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: digital-library-service
            port:
              number: 80